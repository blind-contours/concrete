% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tvCensoring.R
\name{.tvCensLaggedSurv}
\alias{.tvCensLaggedSurv}
\title{Build the lagged censoring-survival matrix with time-varying covariates, in the
core's \code{LaggedCensSurv} convention (matches \code{getHazSurvPred()}), for overriding
the IPCW throughout the pipeline. \code{CensoringTV} is a long data.frame with the
data's id column (\code{attr(Data, "ID")}), a \code{time} column, and value columns.}
\usage{
.tvCensLaggedSurv(
  Data,
  CensoringTV = NULL,
  times,
  Crossover = NULL,
  SL.library = c("SL.mean", "SL.glm"),
  n.folds = 5L,
  nGridCens = 40L
)
}
\value{
a \verb{length(times) x n} matrix of lagged censoring survival.
}
\description{
Build the lagged censoring-survival matrix with time-varying covariates, in the
core's \code{LaggedCensSurv} convention (matches \code{getHazSurvPred()}), for overriding
the IPCW throughout the pipeline. \code{CensoringTV} is a long data.frame with the
data's id column (\code{attr(Data, "ID")}), a \code{time} column, and value columns.
}
\keyword{internal}
