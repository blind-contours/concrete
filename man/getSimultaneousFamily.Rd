% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/getSimultaneousFamily.R
\name{getSimultaneousFamily}
\alias{getSimultaneousFamily}
\title{Joint simultaneous inference across a family of estimands}
\usage{
getSimultaneousFamily(..., Signif = 0.05, nSim = 10000L)
}
\arguments{
\item{...}{two or more \code{"ConcreteOut"} objects produced from the \strong{same}
fitted \code{\link[=doConcrete]{doConcrete()}} object (so that subjects align). Each may be named;
names are carried into the output \code{family} column.}

\item{Signif}{numeric (default 0.05): family-wise alpha.}

\item{nSim}{integer (default 1e4): Monte Carlo draws for the multiplier
bootstrap critical value.}
}
\value{
a \code{data.table} with one row per scalar estimand in the family:
\code{family}, \code{Estimand}, \code{Event}, \code{Time}, \code{Intervention}, \verb{Pt Est}, \code{se}, the
pointwise \verb{CI Low} / \verb{CI Hi}, and the simultaneous \verb{SimCI Low} / \verb{SimCI Hi},
plus the shared simultaneous critical value in \code{attr(., "critValue")}.
}
\description{
Trial reports rarely contain a single number: a typical analysis presents a
risk difference at several horizons, the RMST difference, and a win ratio
together. Pointwise 95\% intervals do not control the family-wise error across
such a set, and a Bonferroni correction is needlessly conservative because the
estimands are computed from the \emph{same} targeted curves and are therefore
strongly correlated. \code{getSimultaneousFamily()} builds \strong{simultaneous}
(family-wise) confidence bands across an arbitrary collection of \code{concrete}
estimands by exploiting that correlation.

Every \code{concrete} estimate carries its per-subject efficient influence values.
Stacking them into one \eqn{n \times q} matrix gives the joint influence
function of the whole family; its empirical correlation \eqn{R} is the
correlation of the (asymptotically normal) estimators. The simultaneous
critical value is the \eqn{1-\alpha} quantile of \eqn{\max_j |Z_j|} for
\eqn{Z \sim N(0, R)} (a multiplier/Gaussian-multiplier bootstrap), and each
band is \eqn{\hat\psi \pm q\,\widehat{\mathrm{se}}} (or, for ratio estimands,
the same on the log scale). When the family is a single estimand at one time
this reduces to the usual Wald interval.

This composes with everything else in the package: the influence values used
are exactly those reported by \code{\link[=getOutput]{getOutput()}}, \code{\link[=getRMST]{getRMST()}}, \code{\link[=targetRMST]{targetRMST()}},
\code{\link[=getWinRatio]{getWinRatio()}}, and \code{\link[=targetWinRatio]{targetWinRatio()}}, so the simultaneous bands inherit the
covariate adjustment, censoring correction, cross-fitting, and (when present)
the stratified-randomization variance correction.
}
\seealso{
\code{\link[=getOutput]{getOutput()}}, \code{\link[=getRMST]{getRMST()}}, \code{\link[=targetRMST]{targetRMST()}}, \code{\link[=getWinRatio]{getWinRatio()}},
\code{\link[=targetWinRatio]{targetWinRatio()}}.
}
