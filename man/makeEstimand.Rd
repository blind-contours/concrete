% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimand.R
\name{makeEstimand}
\alias{makeEstimand}
\alias{print.ConcreteEstimand}
\alias{applyIntercurrentEvent}
\title{Specify an ICH E9(R1) estimand and apply an intercurrent-event strategy}
\usage{
makeEstimand(
  treatment = "as randomized (intent-to-treat)",
  population = "as enrolled",
  endpoint = "cause-specific absolute risk",
  summary = "risk difference",
  strategy = c("treatment policy", "hypothetical", "composite"),
  intercurrent = NULL
)

\method{print}{ConcreteEstimand}(x, ...)

applyIntercurrentEvent(
  Data,
  EventTime,
  EventType,
  Intercurrent,
  strategy = c("treatment policy", "hypothetical", "composite"),
  TargetEvent = 1,
  intercurrent = "intercurrent event",
  Verbose = TRUE
)
}
\arguments{
\item{treatment}{character: the treatment condition / intervention contrast.}

\item{population}{character: the target population.}

\item{endpoint}{character: the variable / endpoint (e.g. cause-specific
absolute risk, RMST) and its horizon.}

\item{summary}{character: the population-level summary (e.g. risk difference,
risk ratio, RMST difference).}

\item{strategy}{one of \code{"treatment policy"}, \code{"hypothetical"}, \code{"composite"}.}

\item{intercurrent}{character or NULL: a label for the intercurrent event.}

\item{x}{a \code{"ConcreteEstimand"} object.}

\item{...}{ignored.}

\item{Data}{a \code{data.table}/\code{data.frame} of one row per subject.}

\item{EventTime}{character: name of the observed-time column.}

\item{EventType}{character: name of the event-type column (\code{0} = censoring,
positive integers = events).}

\item{Intercurrent}{numeric: the event-type value in \code{EventType} that codes
the intercurrent event.}

\item{TargetEvent}{numeric: for the \code{"composite"} strategy, the event-of-
interest code that the intercurrent event is merged into.}

\item{Verbose}{logical: report what was recoded.}
}
\value{
\code{makeEstimand()} returns a \code{"ConcreteEstimand"} object.

\code{applyIntercurrentEvent()} returns a copy of \code{Data} with \code{EventType}
recoded for the chosen \code{strategy}, carrying the estimand in
\code{attr(., "Estimand")}.
}
\description{
\code{makeEstimand()} records the analysis target using the five attributes of the
ICH E9(R1) estimand framework (treatment condition, population, variable /
endpoint, intercurrent-event handling, and population-level summary). It does
not change the estimation; it documents the target for a statistical analysis
plan and travels with the results for reproducibility and regulatory clarity.

\code{applyIntercurrentEvent()} implements the data handling for the
intercurrent-event (ICE) strategy. An ICE is coded as one of the event-type
values in the data (the observed time is the earliest of the outcome,
competing, censoring, and ICE times). Supported strategies:

\describe{
\item{\code{"treatment policy"}}{The ICE is ignored and follow-up continues to
the outcome -- the default intent-to-treat target. The data are returned
unchanged (the ICE code, if any, is treated as a competing/censoring event
exactly as supplied).}
\item{\code{"hypothetical"}}{The ICE is recoded as censoring (event type \code{0}).
\code{concrete}'s inverse-probability-of-censoring weighting then targets the
risk that would be observed if the ICE had not occurred, \strong{under the
assumption that the ICE acts as conditionally independent censoring given
the measured covariates} -- a strong, untestable assumption that should be
accompanied by a censoring sensitivity analysis.}
\item{\code{"composite"}}{The ICE is recoded as an occurrence of the event of
interest \code{TargetEvent}, defining a composite endpoint.}
}

The \code{"while on treatment"} and \code{"principal stratum"} strategies are not
supported: the former is a different (descriptive, non-extrapolated) estimand
that an IPCW estimator does not target, and the latter requires latent
stratification beyond the current scope.
}
