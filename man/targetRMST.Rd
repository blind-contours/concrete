% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/targetRMST.R
\name{targetRMST}
\alias{targetRMST}
\title{Directly target the restricted mean survival time}
\usage{
targetRMST(
  ConcreteEst,
  Horizon = NULL,
  Intervention = seq_along(ConcreteEst),
  TargetEvent = NULL,
  MaxUpdateIter = 50L,
  OneStepEps = 0.1,
  Signif = 0.05,
  NIMargin = NULL,
  NIDirection = c("lower", "upper"),
  EICStopRule = c("hybrid", "relative", "absolute"),
  EICStopAbsTol = NULL,
  Verbose = FALSE
)
}
\arguments{
\item{ConcreteEst}{a \code{"ConcreteEst"} object from \code{\link[=doConcrete]{doConcrete()}}.}

\item{Horizon}{numeric: the restriction horizon \eqn{\tau}. Defaults to the
largest target time; snapped to the nearest target time at or below it.}

\item{Intervention}{numeric (default \code{seq_along(ConcreteEst)}): interventions
to summarize; the first two are treated as treatment and control.}

\item{TargetEvent}{numeric: event types to target. Defaults to the events the
model was fit on.}

\item{MaxUpdateIter}{integer: maximum fluctuation steps.}

\item{OneStepEps}{numeric: initial step size for the fluctuation.}

\item{Signif}{numeric (default 0.05): alpha for confidence intervals and
two-sided Wald p-values.}

\item{NIMargin, NIDirection}{optional non-inferiority margin and direction,
passed to the contrast estimands (see \code{\link[=getOutput]{getOutput()}}).}

\item{EICStopRule}{one of \code{"hybrid"} (default), \code{"relative"}, or \code{"absolute"}:
the stopping rule for the RMST/LYL estimating equation, evaluated on the
rescaled fraction-of-horizon scale.}

\item{EICStopAbsTol}{numeric absolute tolerance for the \code{"absolute"} and
\code{"hybrid"} rules on the fraction scale. Defaults to \code{0.02 / sqrt(n)}.}

\item{Verbose}{logical: print per-step convergence diagnostics.}
}
\value{
a \code{data.table} of class \code{"ConcreteOut"} with the directly-targeted
RMST / life-years-lost estimates. The per-arm convergence status is stored
in \code{attr(., "RMSTConverged")}.
}
\description{
\code{targetRMST()} runs an additional one-step TMLE update that targets the
restricted mean survival time (RMST) and cause-specific life-years-lost (LYL)
estimands \strong{directly}, rather than integrating pointwise-targeted absolute
risks (the approach used by \code{\link[=getRMST]{getRMST()}}).

The estimand \eqn{\mathrm{LYL}_j(\tau)=\int_0^\tau F_j(t)\,dt} is a smooth
time-average of the cumulative incidence, so its efficient influence function
is the time-integral of the pointwise risk influence functions and its clever
covariate is the time-integral of the pointwise clever covariates:
\deqn{H_{l,j,\tau}(s) = \frac{\pi^*(A\mid W)}{\pi(A\mid W) S_c(s^-)}
  \left[\mathbf 1(l=j)(\tau-s) - \frac{\int_s^\tau F_j(t)\,dt - (\tau-s)F_j(s)}{S(s)}\right].}
Targeting this single, well-conditioned functional avoids the dependence on a
dense \code{TargetTime} grid and tends to converge and cover better than the
pointwise approach in rare-event, competing-risk, and long-horizon settings.

Starting from the hazards in \code{ConcreteEst} (already fit by \code{\link[=doConcrete]{doConcrete()}}),
\code{targetRMST()} fluctuates them along \eqn{H} until the empirical mean of the
RMST/LYL influence function is small, then reports the directly-targeted
estimates with influence-function standard errors, p-values, and (optionally)
a non-inferiority assessment.
}
\seealso{
\code{\link[=getRMST]{getRMST()}} for the integrate-pointwise-risks version.
}
