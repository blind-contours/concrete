% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/getRelativeEfficiency.R
\name{getRelativeEfficiency}
\alias{getRelativeEfficiency}
\title{Relative efficiency of a covariate-adjusted vs unadjusted analysis}
\usage{
getRelativeEfficiency(Adjusted, Unadjusted)
}
\arguments{
\item{Adjusted}{a \code{"ConcreteOut"} table from a covariate-adjusted fit.}

\item{Unadjusted}{a \code{"ConcreteOut"} table from an unadjusted (treatment-only)
fit of the same estimands, interventions, events, and times.}
}
\value{
a \code{data.table} keyed by \code{Intervention}, \code{Estimand}, \code{Event}, and
\code{Time} with columns \code{seAdjusted}, \code{seUnadjusted}, \code{RelEfficiency},
\code{VarReductionPct}, and \code{EffSampleSizeMult}.
}
\description{
In a randomized trial, covariate adjustment does not change the target
estimand but it can sharpen it. \code{getRelativeEfficiency()} quantifies that
precision gain by comparing the influence-function standard errors of an
adjusted analysis to those of an unadjusted analysis of the \strong{same} estimand,
which is the quantity the FDA's 2023 covariate-adjustment guidance is about.

Supply two \code{"ConcreteOut"} tables (from \code{\link[=getOutput]{getOutput()}} or \code{\link[=getRMST]{getRMST()}}): one
from a covariate-adjusted fit and one from an unadjusted fit. The unadjusted
fit is the same workflow with treatment-only nuisance models, e.g. a
marginal propensity (\code{"SL.mean"}) and hazard formulas of the form
\code{Surv(time, event == j) ~ arm}.

For each matched estimand the function reports the relative efficiency
\eqn{\mathrm{RE} = \mathrm{Var}_{\text{unadj}} / \mathrm{Var}_{\text{adj}}}
(values above 1 favor adjustment), the implied percentage variance reduction,
and the effective sample-size multiplier: an adjusted analysis on \eqn{n}
subjects has the precision of an unadjusted analysis on \eqn{\mathrm{RE}\,n}.
}
\seealso{
\code{\link[=getOutput]{getOutput()}}, \code{\link[=getRMST]{getRMST()}}
}
