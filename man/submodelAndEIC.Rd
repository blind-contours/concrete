% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/doTmleUpdate.R, R/submodelAndEIC.R
\name{submodelAndEIC}
\alias{submodelAndEIC}
\title{submodelAndEIC}
\usage{
submodelAndEIC(
  Estimates,
  epsVec,
  Data,
  TargetEvent,
  TargetTime,
  Verbose = FALSE
)

submodelAndEIC(
  Estimates,
  epsVec,
  Data,
  TargetEvent,
  TargetTime,
  Verbose = FALSE
)
}
\arguments{
\item{Estimates}{A list of arms, each containing:
\itemize{
\item Hazards: a named list of hazard matrices (rows = times, columns = subjects)
\item EvntFreeSurv: a matrix of survival probabilities
\item NuisanceWeight: a matrix of 1/(g*(censorSurv)) or similar
\item PropScore: stored g-star?
}}

\item{epsVec}{numeric vector of length k = (#arms * #events * #times).}

\item{Data}{data.table with T.tilde, Delta, etc. (as in doTmleUpdate)}

\item{TargetEvent}{numeric vector of event types (e.g. c(1,2))}

\item{TargetTime}{numeric vector of target times (e.g. c(365, 730))}

\item{Verbose}{logical, if TRUE prints some progress}
}
\value{
numeric vector of length k, the Pn(EIC) values

A numeric vector of length k. This is the stack of Pn(EIC_j) for j=1..k.
The root solver tries to make all of them ~0.
}
\description{
Evaluate the empirical mean EIC (length k) for a guessed epsVec, without
modifying your real 'Estimates'.

Applies a multi-dimensional universal least-favorable submodel parameterized
by epsVec. Then returns the k-dimensional vector of empirical means of the
efficient influence curve for each coordinate.
}
